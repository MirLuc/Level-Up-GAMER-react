// level-up-gamer-react/src/config/api.js

// Tu servidor backend se está ejecutando en el puerto 5000 (o el que definiste en el .env del backend)
const BASE_URL = 'http://localhost:5000/api'; 

export default BASE_URL;